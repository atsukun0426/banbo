<% if @error_messages.present? %>
  <div class="alert alert-warning">
    <h2><%= @error_messages.errors.count %>件のエラーが発生しました。</h2>
    <ul>
      <% @error_messages.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
